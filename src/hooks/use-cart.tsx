// Re-export Zustand cart store as useCart hook to maintain compatibility
export { useCartStore as useCart } from '@/store/cart-minimal'

// Placeholder for any future context-based cart functionality
// Currently using minimal cart store to resolve infinite loop